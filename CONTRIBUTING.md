# 参与开发

Papertable 仍处于本地优先知识工作台的原型期。欢迎先以 issue 或设计讨论说明要解决的真实工作流，再提交实现。

## 本地检查

```bash
pnpm install
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

涉及关系、上下文或导入导出的修改，必须补充覆盖正常路径与跨项目隔离的测试。涉及交互的修改，请同时确认键盘操作、焦点、Esc 退出和 390px 移动端布局。

## 贡献原则

- 不加入账号、云端计费、支付或隐式数据上传。
- 不把 Obsidian 或任何单一软件变成唯一格式。
- 不将“看起来像 Explore”当作验收目标；保留产品逻辑，形成独立视觉与文案。
- 不在 UI 层偷偷决定上下文；关系和上下文策略必须可审计、可测试。

公开发布前会补充正式许可证与行为准则；在此之前请不要假定可用于生产或再分发。
