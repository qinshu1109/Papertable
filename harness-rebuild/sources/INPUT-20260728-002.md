---
source_type: prompt
captured_at: 2026-07-28T09:08:12+08:00
processed_by: init-ai-project-workspace
content_sha256: 22ab732f14bc5f6d3a8d8818fea2c68633b3bcbe5dd46d780dc56d01ecd5c650
---

# 原始输入

PLEASE IMPLEMENT THIS PLAN:
# Papertable Harness 13 卡持续执行计划

## 目标

使用 GPT-5.6-sol 逐卡完成 TASK-001～013。主干严格串行，每卡独立 WenzMark 会话、分支和 PR；Codex 负责监控、技术验收、故障修复与合并。完成标准是 TASK-013 门禁通过，所有 WenzMark、Git 与进度文档状态一致。

## 编排与设置

- 主泳道：`001验收 → 002 → 003 → 004 → 005 → 006 → 007 → 008 → 009 → 010 → 用户确认 → 011 → 013`。
- TASK-012 在 TASK-003 合入后进行只读文件重叠预检；只有确认不碰主干共享热点时，才在独立 worktree 并行，否则自动回退串行。
- 将 Rust `readableIds` 数据层校验从 TASK-012 移到 TASK-007；TASK-012 只负责附件生命周期。
- 每张 WenzMark 卡固定：
  - 模型：`gpt-5.6-sol`
  - 推理：`high`；一次修复仍失败时才以 `xhigh` 重跑
  - Goal 模式关闭
  - 串行、立即执行关闭
  - 一次只创建已解锁的下一张卡，不建立自动跑完 13 卡的 Workflow
- 每卡从最新已验收的 `feat/readonly-note-harness-alpha` 建立 `task/TASK-xxx-*` 分支。TASK-012 使用独立 worktree。

## 状态、验收与 Git

- 状态流转：`pending → in_progress → WenzMark awaitingAcceptance → done/blocked`。执行 Agent 不再自行认定 `done`。
- 进度入口：
  - `CURRENT.md`：当前卡、下一卡、阻塞项、最近验收，保持 500 字预算。
  - `tasks/TASK-xxx.md`：任务状态和客观 verify。
  - `logs/TASK-xxx.md`：WenzMark ID、分支、时间、检查点、测试结果、异常与验收结论。
- 每次验收必须同时核对：
  1. WenzMark 数据库、日志、进程和退出码；
  2. Git diff、分支与文件范围；
  3. 任务 verify、实际产物和独立复跑结果。
- 通过后由监督 Codex 使用明确 pathspec 提交，推送并创建 PR；CI 和独立审查通过后合入集成分支，再在 WenzMark 点验收并解锁下一卡。
- 不使用 `git add .`，不纳入现有无关 QA 文件，不依靠运行 Agent 的自述或退出码代替验收。

## 启动阶段

1. 先验收当前处于 `awaitingAcceptance` 的 TASK-001：
   - 独立复跑报告所列检查；
   - 修正 `CURRENT.md` 中“已完成”和“尚未启动”的矛盾；
   - 将 Pi spike 依赖隔离到实验产物，生产根目录不保留 Pi 依赖及其锁文件膨胀；
   - 跟踪工作区与必要证据，但不提交 1.4GB Rust `target/` 或无关未跟踪文件；
   - 通过后创建 TASK-001 PR 并合入。
2. 提交一份治理 PR：
   - 提供 ADR-001～007 的七条摘要和一致性检查，由用户一次确认后统一改为 `accepted`；
   - 完成 TASK-007/012 的 `readableIds` 范围调整；
   - 加入新的任务状态与日志约定。
3. ADR 治理 PR 合入后启动 TASK-002，并按主泳道自动接力。
4. TASK-011 开工前仍暂停并取得用户对删除双阶段代码的即时确认。

## 监控与故障兜底

- 运行期间定期对账日志增长、进程、WenzMark 状态和 Git 状态；出现“进程已结束但仍显示运行中”时只修正状态，不盲目重跑。
- Verify 或 CI 失败：保留证据，在同一分支创建一次定向修复会话；按错误类别最多修复两轮，重复失败则标记 `blocked`。
- 测试偶发失败只允许复跑一次；不能稳定复现通过则不得验收。
- 出现卡外改动、共享文件冲突、预计耗时超过两倍或需要扩大权限时，停止接力并汇报。
- 从 TASK-004 起，每卡在自身 `outputs/task-xxx/` 保存关键事件流 fixture；TASK-013 统一回放。
- TASK-003/008重点验收崩溃恢复；TASK-009/010/012增加 UI 与截图证据；TASK-004/011/013执行全量回归。
- 只有 ADR 确认和 TASK-011 删除门禁需要用户介入；其余技术验收、返工判断和 PR 合入由 Codex负责。

## 默认与边界

- 集成目标分支固定为 `feat/readonly-note-harness-alpha`。
- 默认只有一条泳道；两泳道是通过物理文件隔离检查后的例外。
- 不删除当前 1.4GB 构建缓存，除非用户另行授权。
- TASK-013 PR 合入、最终矩阵通过、13 卡与 WenzMark 状态全部闭合后，才将持续目标标记完成。
