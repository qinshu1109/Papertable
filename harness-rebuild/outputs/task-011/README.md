# TASK-011 deletion evidence

- `no-fallback-replay.json` — schema-v1 objective evidence for failed probe,
  unknown capability, adapter mismatch, provider invalid response, and
  exhausted protocol repair. Every case remains on the native protocol and
  records zero host-search or downgraded-workflow calls.
- `deletion-report.md` — before/after source and bundle measurements, deleted
  symbols, build results, retained historical-string classification, and final
  scope audit.

The TASK-004 A–Q fixture remains inert historical replay data. No production
symbol or test imports it.
