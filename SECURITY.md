# 安全说明

Papertable 的模型请求经只监听 `127.0.0.1` 的本机服务转发。密钥从未提交的 `.env.local` 读取；设置页提交密钥时仅发送给这个本机服务，服务会立即清空页面中的值并以 `0600` 权限写入 `.env.local`。密钥绝不进入浏览器存储、IndexedDB、日志、截图、导出包、构建产物或 Git。

对话和后台任务不能指定上游 URL 或模型名；本机设置端点只接受来自本机页面的配置，且接口地址仅允许 HTTPS 或回环地址的 HTTP。服务只调用固定的 OpenAI-compatible 路径和有限模型任务，不能被当作开放代理使用。已粘贴到聊天或 issue 的密钥都应立即轮换。

如发现原型中的敏感数据泄露风险，请勿在公开 issue 中贴出密钥或个人笔记内容；先私下联系仓库维护者，并附上最小复现信息。
